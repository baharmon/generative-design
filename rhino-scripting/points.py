import rhinoscriptsyntax as rs

point = (0,0,0)
# rs.AddPoint(point)

points = ((0,1,1), (0,2,2), (3,3,3)) # tuple
# points = [[0,1,1], [0,2,2], [3,3,3]] # list
# rs.AddPoints(points)

# rs.AddPointCloud(points)

# rs.AddPointCloud(points)

# rs.AddPointCloud(points, colors=(255, 255, 255))
